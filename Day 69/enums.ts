// Define an enum for colors
enum Colors {
    Red,
    Green,
    Blue
  }
  
  // Assign a color
  const chosenColor: Colors = Colors.Green;
  
  // Log the chosen color
  console.log(`The chosen color is ${Colors[chosenColor]}`);
  